
const start={
    loadMsg:'正在加载中...',
    name:'AceApi'
}

const mainList=(state={def:start},action) =>{
    switch (action.type){
        default:
            return state;
    }
}

export default mainList;